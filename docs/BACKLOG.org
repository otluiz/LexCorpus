#+TITLE: LexCorpus — Backlog
#+AUTHOR: Othon Luiz
#+STARTUP: overview
#+TODO: TODO EM-ANDAMENTO BLOQUEADO | FEITO CANCELADO

* Sobre o arquivo
  Backlog do LexCorpus em org-mode (mesmas convenções do BACKLOG do LexLearn).

  Estados (na linha do item, após os **):
  - TODO         → livre para pegar
  - EM-ANDAMENTO → alguém já está fazendo (confirme antes de pegar)
  - BLOQUEADO    → depende de algo (ver descrição); não começar ainda
  - FEITO        → concluído
  - CANCELADO    → descartado

  Prioridade: [#A] alta, [#B] média, [#C] baixa (após o estado).

  Como pegar uma tarefa:
  1. Escolha um item TODO (não EM-ANDAMENTO nem BLOQUEADO)
  2. Prefira [#A] > [#B] > [#C]
  3. Marque-o EM-ANDAMENTO (C-c C-t) antes de começar
  4. Cheque se tem "ALINHAR com o LexLearn" na descrição — itens que
     tocam o contrato (docs/CONTRATO.md) não se mudam unilateralmente (§8)

  Atalhos Emacs: C-c C-t alterna estado · C-c , prioridade · C-c C-c checkbox

* Decisões recentes (contexto para quem chega)
  - MONOLITO MODULAR: 1 repositório, 1 imagem Docker, N containers/jobs.
    NÃO um container/imagem por banca. O que varia por banca é spider
    (código) e agenda (config), não artefato. Formalizar em ADR-0002.
  - PCI CONCURSOS: usar SÓ como índice de descoberta (quais concursos
    existem, qual banca, qual ano). Os PDFs vêm da fonte primária (site
    oficial da banca), como manda o contrato §1. Motivos: Turnstile
    (Cloudflare) protege as URLs reais de download e o robots.txt do PCI
    tem "Disallow: /*.pdf". Formalizar em ADR-0001.
  - FCC: os cadernos de prova NÃO são públicos (portal do candidato,
    acesso individual por hash de caderno). A fonte primária FCC entrega
    os GABARITOS oficiais (saem como editais públicos ao fim do certame).
    robots.txt da FCC bloqueia /concursos/ e *.pdf → crawls rodam com
    -s ROBOTSTXT_OBEY=False (decisão assumida, ver docstring do spider).
  - HEURÍSTICAS EM MÓDULO (ADR-0004, 07/08): a decisão é o módulo de
    funções puras lexcorpus/heuristics.py + base fina que só DELEGA
    (make_item + repasse dos hooks RE_*). base.py ALINHADA em 09/08:
    a duplicação foi eliminada e a classificação delega ao módulo;
    override por banca = sobrescrever o hook de classe (regime FORTE,
    igual ao parâmetro do módulo). Restam as cópias locais de
    fgv.py/fcc.py/pci.py (cebraspe.py é caso à parte: classifica por
    descricaoArquivo da API, não por heurística). Convergência no item
    "Migrar pci.py e fcc.py para heuristics.py".
  - DOCS: os .md migram para docs/; na raiz fica só o README.md.
    Este backlog vive em docs/BACKLOG.org.
  - PROVA NÃO EXPIRA (18/08): FILES_EXPIRES passou de 90 para 700 dias E,
    mais importante, o FilesPipeline ganhou GATE ADMINISTRATIVO
    (_onsuccess em pipelines.py): arquivo "expirado" NÃO é rebaixado/
    sobrescrito automaticamente — o crawl mantém o arquivo e loga WARNING
    pedindo decisão do administrador, que autoriza com
    -s LEXCORPUS_REBAIXAR_EXPIRADOS=True. Os arquivos são o acervo do
    LexLearn (DNA das bancas); substituição nunca é efeito colateral de
    cache. Política de expurgo, se um dia existir, será objeto de estudo.

* Em andamento / próximos
** FEITO [#A] StateStore + ciclo preliminar→definitivo (contrato §6.11)
   CLOSED: 2026-08-31
   O contrato modela o ciclo (vigente, substituido_por, concurso.
   atualizado) mas NADA disso está implementado: hoje toda coleta publica
   concurso.disponivel com vigente=true, e a routing key
   RABBIT_ROUTING_ATUALIZADO existe no settings sem uso. O cache de
   download dura 700 dias e, mesmo vencido, o pipeline segura o arquivo
   para decisão do administrador (gate 18/08) — mas nada sabe dizer "o
   definitivo chegou".
   Solução: SQLite pequeno no volume de estado.
   - [X] Tabela arquivo_visto(banca, concurso, nome, papel, checksum,
         vigente, first_seen, last_seen) — FEITO 31/08:
         lexcorpus/statestore.py (WAL + busy_timeout para os crawls
         paralelos do scheduler; caminho via LEXCORPUS_STATE_DB)
   - [X] EventoRabbitPipeline consulta o estado antes de agregar:
         concurso/arquivos novos → concurso.disponivel (como hoje);
         surgiu gabarito_definitivo onde havia preliminar → marca o
         preliminar vigente=false + substituido_por e publica o conjunto
         como concurso.atualizado; nada mudou → não publica nada.
         FEITO 31/08: diff em _fechar_concurso (pipelines.py); matching
         preliminar↔definitivo por interseção de cargos ("*" cobre tudo)
         + tipo_prova; sidecar do preliminar regravado (só metadado)
   - [X] Idempotência de verdade do lado produtor (não depender só do
         event_id no consumidor) — "nada mudou → não publica nada"
   - [X] Re-run diário fica barato e seguro (só publica delta)
   - ALINHAR com o LexLearn: a semântica do concurso.atualizado está no
     contrato; validar o primeiro evento real com um consumidor de teste
     → segue ABERTO no item §9 "Validar o primeiro concurso.atualizado real"
   - Fecha o ponto aberto "Ciclo preliminar → definitivo" do
     docs/COMUNICADO_INTEGRACAO_OK.md
   Testes: tests/test_statestore.py (5 casos, sem rede/RabbitMQ; eventos
   validados contra o schema). 88 testes verdes em 31/08.

** TODO [#B] Migrar pci.py e fcc.py para heuristics.py (ADR-0005)
   Plano formalizado no ADR-0005 (09/08): ordem, pré-requisitos
   encadeados (bug do "oficial" primeiro) e critérios de aceite.
   lexcorpus/heuristics.py (07/08) centraliza regex + classificar_papel +
   eh_relevante como funções puras; agregador_generico/estrategia usam
   via base fina (delegação desde 09/08). Restam os dois spiders antigos.
   - [ ] pci.py: trocar regex locais por import de heuristics; ATENÇÃO —
         o pci ainda tem a versão FRACA de eh_relevante (ver item
         "Falso positivo")
   - [ ] fcc.py: heuristics.classificar_papel(..., re_descartar=FORTE) —
         o descarte forte (resultado|convoca|reclassifica|... vence
         "prova" no nome) vira PARÂMETRO, não cópia. O desempacote rybena
         (?file=) PERMANECE no spider — é da fonte, não é heurística
   - [ ] Generalizar o descarte forte? Avaliar se vira default do módulo
         (caso de teste: edital_de_resultado_preliminar_prova_objetiva.pdf)
   - [ ] Testes unitários das heurísticas contra nomes reais coletados
         (FGV, FCC, CEBRASPE) — agora sem instanciar spiders

** TODO [#B] Validar spiders estrategia e agregador_generico em crawl real
   Código entregue 07/08 (ver histórico), mas os seletores CSS foram
   escritos contra padrões comuns de blog/WordPress — precisam de
   validação contra o HTML real antes de entrar no watchlist.
   Validação parcial em 09/08 (fetch direto da listagem + 1 post real,
   parse com os seletores do spider, sem crawl completo):
   - [X] estrategia: descoberta de posts funciona (busca "prova
         gabarito" retorna 155 links, 55 posts após dedup) — MAS o
         filtro "/blog/" in url é permissivo demais: páginas
         /blog/author/* entram como se fossem posts (crawl desperdiçado)
   - [ ] estrategia: PAGINAÇÃO QUEBRADA — nenhum seletor de next
         (a.next, a[rel=next], link[rel=next], .pagination a:last-child)
         casa o HTML real da busca; o spider para na página 1 mesmo com
         -a paginas=5. Inspecionar o markup real de paginação e ajustar
   - [ ] estrategia: post recente de "provas aplicadas" (Sefaz CE 2026)
         NÃO tem links a[href$='.pdf'] de gabarito — só edital/
         retificações (descartados corretamente). Verificar como os
         gabaritos são publicados no blog (link sem extensão? botão
         JS?); o pdf_selector pode precisar de ampliação
   - [ ] estrategia: _inferir_banca retornou "Desconhecida" em título
         sem nome de banca ("Concurso Sefaz CE: ...") — limitação
         esperada; avaliar fallback buscando a banca no corpo do post
   - [ ] agregador_generico: ensaio contra 1 agregador real (candidato:
         listagem pública sem captcha; PCI continua sendo descoberta, ver
         ADR-0001) e fixture de parse em tests/
   - [ ] Se os seletores divergirem: ajustar defaults e documentar no
         docstring

** TODO [#B] Cadeia de técnicas de extração com fallback (LexCorpusSpider)
   Ideia (07/08): a base tenta MAIS DE UMA técnica de extração; se uma
   retorna zero itens, cai para a próxima. Subclasses declaram/ordenam
   as técnicas. Análise registrada em 07/08:
   - NÍVEL 1 (mesmo response, barato — fazer primeiro): técnicas são
     funções puras (response -> [candidatos]). Encadear na base é trivial
     porque dá para saber na hora se extraiu 0 itens:
     1. seletores CSS configurados (comportamento atual)
     2. varredura genérica de todo a[href$='.pdf'] da página
     3. JSON embutido (script[type='application/ld+json'], __NEXT_DATA__)
   - NÍVEL 2 (fallback ENTRE requests — adiar): tentar outro endpoint/
     página se o primeiro não rendeu exige saber o resultado só no fim
     da resposta; no Scrapy isso é agendamento preguiçoso + contador de
     sucesso. Complexidade real; implementar só quando um caso concreto
     aparecer (ex.: SPA sem JSON acessível).
   - Interface proposta: subclasses declaram `tecnicas = [método, ...]`;
     a base itera até a primeira não-vazia, sempre passando pelo gate
     final classificar_papel (evita falso positivo da técnica genérica)
   - Observabilidade: spider.stats.inc_value("tecnica/{nome}") — mede
     qual técnica resolve cada fonte e denuncia seletor quebrado
   - [ ] Implementar NÍVEL 1 na base + refatorar agregador_generico para
         declarar técnicas em vez de if/else
   - [ ] Cada técnica como função pequena testável (alimenta o CI)

** TODO [#B] Spiders de descoberta (listagens, sem download)
   Um spider por portal que percorre SÓ as listagens e emite "concurso
   candidato" (banca, órgão, ano, URL). Alimenta o watchlist. Não baixa
   PDF, então é leve e roda diário sem pesar os sites.
   - [ ] PCI /provas/ + /provas/{cargo} (sem captcha — verificado 05/08):
         o agregador mais rico; descobre concursos de TODAS as bancas.
         Candidato a rodar via agregador_generico parametrizado (07/08)
   - [X] FGV /concursos (slugs ativos) — FEITO 18/08 via modo descoberta
         do próprio spider fgv (-a descoberta=1 → eventos_debug/descoberta/
         fgv.jsonl, 339 slugs em 13 páginas)
   - [ ] FCC raiz do concursosfcc.com.br
   - [ ] Saída: JSONL em eventos_debug/descoberta/ (ou fila própria depois)
   - É o uso correto do PCI: índice, não fonte de arquivo (ver ADR-0001)

** BLOQUEADO PCI como fonte de PDFs (Turnstile + robots.txt)
   Página de download tem ZERO links .pdf: os arquivos ficam atrás de um
   POST /provas/link que exige token cf-turnstile-response válido. Teste
   com Chromium headless (05/08): o Turnstile NÃO liberou token sozinho.
   Agravante: robots.txt do PCI tem "Disallow: /*.pdf" e o spider roda
   com ROBOTSTXT_OBEY=True — mesmo achando link, o Scrapy não baixaria.
   - Decisão tomada: PCI é fonte de DESCOBERTA, não de arquivos (ADR-0001)
   - Reabrir SOMENTE se: (a) fizer sentido jurídico/comercial burlar uma
     proteção técnica explícita, E (b) houver serviço de resolução de
     captcha (2Captcha/CapSolver resolvem Turnstile) no orçamento
   - O contrato §1 já declara fonte primária como princípio — o PCI
     sempre foi exceção, não regra
   - NOTA 05/08: para PROVAS FCC (cadernos não públicos no site oficial),
     o PCI segue sendo a única fonte de cadernos — reforça a tensão
     deste item com a decisão "PCI só descoberta". Resolver no ADR-0001.
   - CONFIRMADO 09/08: Turnstile continua ativo. Página real de download
     (/provas/download/administracao-assembleia-legislativa-rs-fundatec-2026)
     retorna 200 com cf-turnstile-response + POST /provas/link e ZERO
     links <a ... .pdf> — o spider varre e não acha nada (warning
     "nenhum PDF encontrado"). É o comportamento esperado, não bug:
     `scrapy crawl pci` sem resultado = este bloqueio em ação.

* Pipeline / correções
** FEITO [#A] Config de ambiente: USER_AGENT real e RABBIT_URL por env
   CLOSED: 2026-08-31
   Profissionalização básica antes de rodar automático contra as bancas.
   - [X] USER_AGENT hoje é "lexcorpus/0.1 (+contato@exemplo.br)" —
         placeholder. Colocar contato real (boa-fé com os sites + exigido
         por vários ToS). FEITO 31/08: contato real (olto@ecomp.poli.br,
         e-mail do autor no git config) em settings.py
   - [X] RABBIT_URL hardcoded amqp://guest:guest@localhost → ler de env
         (padrão FILES_STORE que já usa os.environ). FEITO 05/08 junto
         com RABBIT_ENABLED: settings.py lê os.environ.get para ambos;
         default local inalterado (False / localhost)
   - [X] Credenciais de produção fora do repo (.env + .env.example)
         FEITO 30/08: .env.example na raiz (template comentado; exceção
         !.env.example no .gitignore) + seção "Configuração por ambiente"
         no README. EVENTOS_OUT_DIR passou a ler de env também.

** FEITO [#A] BUG: "Gabarito Oficial Preliminar" vira gabarito_definitivo
   CLOSED: 2026-08-13
   Reproduzido em 09/08: RE_GAB_DEF inclui "oficial" como qualificador
   de definitivo, então "Gabarito Oficial Preliminar" (rótulo padrão da
   FGV) casa RE_GAB_DEF antes de RE_GAB_PRE. Afeta heuristics.py →
   base.py delega nela (desde 09/08), então estrategia.py e
   agregador_generico.py herdam o erro. fgv.py NÃO é afetado (regex
   locais estritas: gabarito\s+oficial\s+definitiv / \s+preliminar).
   - [X] Corrigir a precedência EM heuristics.py (cópia única desde o
         alinhamento da base): checar RE_GAB_PRE ANTES de RE_GAB_DEF,
         ou remover "oficial" dos qualificadores de definitivo (um
         gabarito "oficial" pode ser preliminar OU definitivo)
         FEITO 13/08: as duas proteções foram aplicadas — RE_GAB_PRE
         roda antes de RE_GAB_DEF e "oficial" saiu de RE_GAB_DEF.
   - [X] Casos de teste: "Gabarito Oficial Preliminar" → preliminar;
         "Gabarito Oficial Definitivo" → definitivo; "Gabarito" (sem
         qualificador) → definitivo (regra vigente)

** TODO [#B] Falso positivo: editais de resultado com "prova" no nome
   A heurística de descarte só descarta se NÃO houver pista de
   prova/gabarito. Na FCC, editais de resultado/convocação/reclassificação
   mencionam "prova_objetiva" no nome do arquivo → passam pelo filtro e
   são classificados como papel=prova (errado). O LexLearn reclassifica
   com autoridade, mas o coletor não deve poluir o evento.
   - [X] No fcc.py (05/08): _RE_DESCARTAR forte (resultado|convoca|
         reclassifica|homologa|inscri|condicoes_espec|habilitad) vence
         mesmo havendo "prova" no nome — validado no crawl dpeba125
         (26 descartes corretos, incluindo a armadilha
         "prova_e_condicoes_especificas_deferidas.pdf")
   - [ ] Generalizar: levar a regra para spiders/base.py (como override
         de classe ou novo default) — a base de 07/08 ainda tem a versão
         fraca; o pci.py idem. Ver item "Migrar pci.py e fcc.py"
   - [ ] Caso de teste: edital_de_resultado_preliminar_prova_objetiva.pdf

** TODO [#B] Associação arquivo → cargo em concursos multi-cargo
   Ponto aberto do comunicado de integração: hoje arquivos de concurso
   multi-cargo são marcados com o conjunto TODO de cargos, sem discriminar
   por arquivo (multi_cargo=true). Melhorar onde a fonte permitir.
   - [ ] FCC: o edital de gabaritos cobre vários cargos num único PDF —
         hoje vai cargos=["*"], multi_cargo=true (contrato §5, caso C).
         Extrair cargo exigiria parsear o PDF, o que é papel do LexLearn
   - [ ] FGV: a página costuma agrupar prova por cargo — capturar o
         contexto do link (cabeçalho da seção, texto do link)
   - [ ] CEBRASPE: a API não amarra arquivo→cargo; manter ["*"] ou todos
   - [ ] agregador_generico/estrategia (07/08): o texto do link vira
         rótulo de cargo — heurística razoável, mas revisitar com dados
         reais na validação dos spiders
   - [ ] Não inventar: se não der para extrair, omite (contrato §5, caso C)
   - Não bloqueia nada; refinamento contínuo

** TODO [#C] "Padrão de resposta" da discursiva hoje vira papel=prova
   Registrado no comunicado como comportamento conhecido. O LexLearn tem
   autoridade para reclassificar. Revisitar só se virar ruído real.

* Docker / profissionalização
** FEITO [#A] Dockerfile multi-stage (base + base-playwright)
   CLOSED: 2026-08-31
   UMA imagem para todo o projeto; um segundo target adiciona Chromium+
   Playwright só para os spiders que precisam (Cesgranrio; PCI se um dia
   for reaberto). Imagem principal fica enxuta.
   NOTA 18/08: a Cesgranrio foi desbloqueada SEM Playwright (API JSON
   pública — ver item FEITO no histórico). Hoje NENHUM spider ativo
   precisa de browser; o stage playwright fica só para o PCI, se reaberto.
   - [X] Stage base (versão inicial 05/08): python:3.11-slim +
         requirements.txt + código, ENTRYPOINT scrapy, .dockerignore
         excluindo venv/.git/eventos_debug. NOTA 31/08: o .dockerignore
         não existia de fato no repo — criado agora (.git, venv,
         eventos_debug, state, tmp, docs, legado)
   - [X] Stage playwright: base + playwright install --with-deps chromium.
         FEITO 31/08: Dockerfile multi-stage — base (default, == final),
         playwright (--target playwright), final. O compose do LexLearn-v3
         não passa target, então o default segue enxuto
   - [X] Volume /data/raw/exams (contrato: o LexLearn monta o mesmo) —
         validado no compose de integração
   - [X] Volume /state (SQLite do StateStore) e /app/watchlist.yaml
         (ou copiar no build e permitir override por volume).
         FEITO 31/08: VOLUME /state + ENV LEXCORPUS_STATE_DB e
         LEXCORPUS_STATE_FILE apontando para lá

** EM-ANDAMENTO [#A] docker-compose de dev/prod (scheduler + rabbitmq + volumes)
   Containers por FUNÇÃO, todos da mesma imagem (decisão: monolito
   modular). Nada de um serviço por banca — banca é linha do watchlist.
   - [X] Integração com LexLearn-v3 (05/08): docker-compose.crawler.yml
         na raiz do LexLearn-v3 — override aditivo (base intocado),
         profile "crawler", mesma rede do projeto lexlearn-v3, mesmo
         bind ./data:/data do worker, RABBIT_URL=rabbitmq:5672
         SUPERADO 01/09: o arquivo mudou de casa — ver o checkbox
         "COMPOSE MUDOU DE CASA" abaixo
   - [X] LEXCORPUS_DIR no .env do LexLearn-v3 (caminho relativo até o
         repo LexCorpus, ex.: ../../LexCorpus)
         FEITO: =.env:109= do LexLearn-v3 = =../../LexCorpus=. O DEFAULT
         do compose (=${LEXCORPUS_DIR:-../LexCorpus}=) aponta para
         =LexLearn/LexCorpus=, que NÃO existe nesta máquina — quem clonar
         sem o =.env= quebra no build, e a mensagem não diz por quê.
         MORTO 01/09: a variável deixou de existir na mudança de casa
         (build.context virou =.=)
   - [X] COMPOSE MUDOU DE CASA (01/09): docker-compose.crawler.yml veio
         do LexLearn para a raiz DESTE repo, e nasceu ao lado o
         docker-compose.lexlearn.yml (a ponte). Razão de arquitetura:
         o ADR-0006 prevê DOIS pontos de contato de runtime — o
         diretório compartilhado e a fila; o build.context apontando
         para o fonte era um TERCEIRO, de build, e morreu:
         - build.context: =${LEXCORPUS_DIR:-../LexCorpus}= → =.=
         - storage: =./data:/data= → bind em sintaxe longa com
           =${LEXCORPUS_STORAGE_DIR:?...}= — SEM DEFAULT, de propósito:
           variável ausente grita, caminho errado se calaria (a forma
           curta quebrava: os dois-pontos do =:?= viravam separador
           host:container)
         - =depends_on: rabbitmq= REMOVIDO — o broker é de outro
           projeto compose; depender de serviço inexistente derruba o
           projeto inteiro ("depends on undefined service"). Sem espera
           pelo broker: a garantia é a pré-condição documentada (stack
           do LexLearn de pé antes)
         - docker-compose.lexlearn.yml: =name: lexcorpus= (o volume de
           estado é sempre =lexcorpus_lexcorpus-state=, sem depender do
           nome do diretório) + rede default externa
           (=lexlearn-v3_default= — resolve "rabbitmq" no broker de lá)
         Validado com =docker compose config= nos dois casos (sem a
         variável recusa nomeando-a; com ela resolve tudo) — reverificado
         em 04/09, com o mesmo resultado.
   - [X] Volume =lexcorpus-state= PROVADO EM DADO REAL (01/09). A linha
         =lexcorpus-state:/state= entrou no =docker-compose.crawler.yml=
         em 31/08 (LexLearn-v3 =d50846d=); hoje o volume foi lido:
         | linhas em =arquivo_visto= | 20 (cebraspe/prf_21) |
         | =first_seen= distintos    | 1 — 01:40:39 |
         | =last_seen= distintos     | 1 — 01:45:40 |
         UM =first_seen= e UM =last_seen=, cinco minutos entre eles: o
         segundo =run --rm= ACHOU as 20 linhas do primeiro e só avançou o
         =last_seen=. Volume não-persistido daria banco novo e vazio, com
         =first_seen == last_seen=. Não é plausibilidade de YAML: são dois
         contêineres efêmeros e a memória entre eles.
         E o CICLO §6.11 rodou no caminho de produção, não em teste: 3
         gabaritos preliminares com =vigente=0= e
         =substituido_por=Gab_Definitivo_085_PRFCF_001_01.pdf=.
         A cadeia do caminho foi conferida DENTRO do contêiner, não
         suposta — =scrapy settings --get LEXCORPUS_STATE_DB= devolve
         =/state/lexcorpus_state.db= (Dockerfile ENV -> =settings.py=
         =os.environ= -> setting do Scrapy que o pipeline lê). Importa
         porque o pipeline lê SETTING, não env: se o =settings.py= não
         fizesse a ponte, o ENV do Dockerfile seria decoração e o estado
         cairia em =/app/state=, fora do volume, calado.
   - [ ] Teste e2e via compose: crawl fcc → evento consumido pelo worker
         do LexLearn
   - [ ] Serviço scheduler: command python -m lexcorpus.scheduler;
         env RABBIT_ENABLED=True, RABBIT_URL, LEXCORPUS_FILES_STORE
   - [ ] Serviço rabbitmq:3-management (dev; em prod pode ser externo)
   - [ ] Rede e depends_on mínimos; profiles para dev vs prod

** TODO [#B] CI: pytest no GitHub Actions
   Já há tests/ (test_e2e, test_cebraspe_parse, test_pci_parse).
   - [ ] Workflow: push/PR → pip install -r requirements.txt → pytest
   - [ ] Validar schemas (evento/sidecar) em todo PR que tocar pipelines
   - [ ] Adicionar fixture de parse da FCC (página real de concurso,
         com links rybena embrulhados)
   - [ ] Adicionar testes dos módulos novos (07/08): heuristics.py
         (unitário puro, sem spider), agregador_generico e estrategia
         (fixtures de parse)
   - [ ] Cache de pip; matrix só se necessário (uma versão basta por ora)

** TODO [#C] .env.example + settings por ambiente
   Documentar LEXCORPUS_FILES_STORE, RABBIT_URL, RABBIT_ENABLED,
   EVENTOS_OUT_DIR. O settings.py já lê FILES_STORE, RABBIT_ENABLED e
   RABBIT_URL de env (desde 05/08) — resta documentar e descrever no
   README.

* Documentação
** FEITO [#A] Mover .md para docs/ e corrigir links
   CLOSED: 2026-08-31
   Decisão tomada: raiz só com README.md.
   - [X] git mv CONTRATO.md COMUNICADO_INTEGRACAO_OK.md docs/
   - [X] Este backlog: docs/BACKLOG.org (conteúdo já em org-mode)
   - [X] README: atualizar TODAS as referências (hoje cita CONTRATO.md,
         schema/, consumer_teste.py com paths da raiz)
   - [X] Referências cruzadas dentro dos próprios docs (o comunicado cita
         CONTRATO.md e consumer_teste.py) — FEITO 30/08: comunicado passa a
         citar docs/CONTRATO.md e tests/consumer_teste.py; README:7 idem
         (docs/CONTRATO.md). Demais docs conferidos (schema/, spiders/,
         tests/ citados já estavam corretos).
   - [X] Avisar o time LexLearn: o contrato mudou de path no repo.
         FEITO 31/08: referências atualizadas no LexLearn-v3 —
         backend/app/workers/lexcorpus_consumer.py (comentário de cabeçalho)
         e docs/architecture/decisions/ADR-011 passam a citar
         docs/CONTRATO.md + schema/*.json no repo LexCorpus

** TODO [#B] Escrever os ADRs iniciais (docs/architecture/decisions/)
   O LexLearn já tem cultura de ADR; trazer para cá. Formato: 4 dígitos.
   - [ ] ADR-0001: agregador como índice, fonte primária como origem de
         arquivos (absorve a decisão PCI: Turnstile + robots.txt).
         NOVO 05/08: incluir o caso FCC — cadernos não públicos na fonte
         primária → de onde vêm as provas FCC? (tensão registrada no
         item BLOQUEADO do PCI)
   - [ ] ADR-0002: monolito modular — uma imagem, N containers por função
         (rejeita container-por-banca; justificativa: o que varia é
         código+config, não artefato)
   - [ ] ADR-0003: watchlist + StateStore como mecanismo de automação e
         do ciclo preliminar→definitivo
   - [ ] ADR-0004: heurísticas em classe base (LexCorpusSpider) em vez de
         módulo solto — registra a decisão de 07/08 e o padrão de
         override por regex de classe; mencionar a cadeia de técnicas
         com fallback como evolução prevista

** FEITO [#B] Atualizar README (novos spiders, automação, docs/)
   CLOSED: 2026-08-31
   Depois dos itens acima:
   - [X] Estrutura: adicionar scheduler.py, spiders/base.py,
         agregador_generico.py, estrategia.py, watchlist.yaml,
         Dockerfile, docker-compose (parcial 09/08: spiders, heuristics,
         base e ADRs entram; scheduler.py ainda não existe — ver item
         próprio)
   - [X] Tabela de bancas suportadas e status (cebraspe ✅, fcc ✅
         gabaritos, fgv em construção, cesgranrio bloqueada, pci
         descoberta, estrategia/agregador_generico em validação) — 09/08
   - [X] Seção "Rodar": modo manual (scrapy crawl) E modo automático
         (scheduler + watchlist); modo container ao lado do LexLearn
         (docker-compose.crawler.yml) — depende do scheduler existir.
         FEITO 31/08: manual e automático já estavam; adicionada a seção
         "Rodar (modo container — ao lado do LexLearn)"
   - [X] Seção "Por onde começar": docs/CONTRATO.md → docs/BACKLOG.org →
         ADRs → schema/

* Alinhar com o LexLearn (contrato §9 — não mexer unilateralmente)
** TODO Vocabulário de slugs de cargo
   Normalização fica no LexLearn, mas convém combinar os mais comuns.
   Com FGV/FCC entrando, colher rótulos reais e propor a lista inicial.
** TODO Nomes definitivos de exchange e routing keys
   lexcorpus.events / concurso.disponivel / concurso.atualizado são
   proposta v2.0. Confirmar antes do StateStore começar a publicar
   concurso.atualizado de verdade.
** TODO Nível de detalhe viável em segmentos
   Depende do que os scrapers extraem por banca. CEBRASPE não amarra;
   FCC entrega gabarito consolidado multi-cargo; FGV pode dar
   página/seção. Combinar o mínimo útil.
** TODO Validar o primeiro concurso.atualizado real
   Quando o StateStore existir: rodar um ciclo preliminar→definitivo
   real (FGV ou FCC são boas candidatas, publicam rápido) com o
   consumidor de teste do LexLearn ouvindo.

* Feito (histórico recente)
** FEITO [#A] Watchlist YAML + scheduler (a automação)               :pipeline:
   CLOSED: 2026-08-26
   lexcorpus/scheduler.py: lê watchlist.yaml, calcula alvos vencidos pelo
   cron de cada um (croniter — tick mais recente vs último disparo) e
   dispara os crawls. Decisões que divergem do enunciado original:
   - Disparo via SUBPROCESSO (`scrapy crawl ...`), não CrawlerProcess
     em-processo: um crawl travado não derruba o scheduler nem os das
     outras bancas, e cada crawl nasce com reactor limpo
   - Último disparo em JSON próprio (./state/scheduler_state.json;
     --state-file / LEXCORPUS_STATE_FILE; volume /state no Docker) —
     o StateStore SQLite completo fica no item próprio (ciclo
     preliminar→definitivo)
   Mantido do enunciado: sequencial por banca + bancas em paralelo
   (uma thread por grupo), trava anti-overlap por alvo, seção
   "descoberta" suportada (os spiders de listagem em si são o item
   "Spiders de descoberta"), alvo que falha não atualiza estado →
   retenta na próxima verificação. Modos: --once (default; cron/
   supercronic no container) e --loop SEG. Deps novas: croniter, PyYAML.
   Watchlist corrigido na mesma passada: fcc params codigo→slug (o
   spider aceita slug), comentários FGV/FCC/Cesgranrio atualizados
   (spiders prontos; Cesgranrio via evento_id, sem esqueleto).
   Testes: tests/test_scheduler.py (15 casos, subprocess mockado).
   Dry-run real validado (só cebraspe ativo → 1 comando).
** FEITO ADR-0006: busca por palavra-chave + catálogo + orquestrador :pesquisa:
   CLOSED: 2026-08-25
   Entregue em 19/08 (buscador.py, catalogo.py, orquestrador.py +
   catalogo.yaml, tests/test_buscador.py), faltava o registro aqui e a
   varredura de consistência pós-Cesgranrio. Fechamento 25/08:
   - orquestrador: SPIDERS_POR_BANCA["cesgranrio"] aponta para o spider
     real (API pública, -a evento_id=N) — não mais "BLOQUEADA/WAF".
     montar_comando generalizado: params fora de url/slug/banca/cargo
     (ex.: evento_id) vão direto ou viram placeholder <PREENCHER> com
     aviso ao operador
   - ADR-0006: errata no topo registrando que a premissa "Cesgranrio
     bloqueada por WAF" caducou em 18/08 (API JSON pública, sem
     Playwright); o mecanismo esqueleto+Playwright fica para WAFs futuros
   - README: removida a linha duplicada "cesgranrio.py ESQUELETO"
** FEITO CLI de consulta ao storage (lexcorpus/consulta.py)        :pipeline:
   CLOSED: 2026-08-25
   Lado leitor do storage: varre os sidecars (*.meta.json) no layout do
   contrato §6.9 ({banca}/{concurso}/) e filtra por banca (exata),
   concurso (trecho), cargo (inclui consolidados multi-cargo "*", §5) e
   papel ("gabarito" abrange preliminar + definitivo). --copiar DIR leva
   PDF + sidecar. Storage via --store / LEXCORPUS_FILES_STORE / default
   Docker — mesma precedência do settings.py. Não baixa nada da rede.
   Validado contra o storage real (cesgranrio/bnb0124, fgv/cnu_2025,
   fcc/dpeba125) + tests/test_consulta.py (10 casos, storage fake em
   tmp_path).
** FEITO Spider Cesgranrio — API pública do portal (sem Playwright)  :spiders:
   CLOSED: 2026-08-18
   Estava BLOQUEADO desde 05/08 (403 do Azure Front Door para qualquer
   cliente não-navegador). Re-diagnóstico 18/08: o site foi reformulado,
   o WAF não bloqueia mais HTTP simples e a SPA do portal consome uma API
   JSON PÚBLICA — Playwright desnecessário (stage playwright do Dockerfile
   perde a única banca que o justificava por ora). spiders/cesgranrio.py:
   GET /api/PortalEventos/{id} + /api/PortalEventoConteudos/publico/{id};
   blocos {titulo, texto(HTML)} com links Azure Blob (SAS próprio, válido
   até 2036). Classificação pelo termo inicial do link + título do bloco
   (PROVAS→prova, GABARITOS→preliminar, GABARITO FINAL→definitivo);
   descarte FORTE via hook RE_DESCARTAR (armadilhas cobertas: "LISTAGEM
   GERAL - PROVA DE TÍTULOS" não é prova; "ANALISTA" não pode casar
   "lista"). Cargo extraído do texto do link preservando numeral romano
   final ("TÉCNICO BANCÁRIO I" ≠ "III"). Validado: parse seco nos 12
   eventos ativos + crawl real BNB 01/2024 (evento_id=10): 5 PDFs (3
   provas, 1 gabarito preliminar, 1 definitivo), sidecars e evento v2.0.
   Eventos 100% digitais/em andamento retornam 0 itens (normal).
   Teste: tests/test_cesgranrio_parse.py. Uso:
   scrapy crawl cesgranrio -a evento_id=N (ids em /api/PortalEventos).
** FEITO [#A] Spider FGV (conhecimento.fgv.br)                       :spiders:
   CLOSED: 2026-08-18
   Site Drupal, HTML estático, SEM captcha (verificado 05/08). Página do
   concurso: /concursos/{slug} (ex.: dataprev26). PDFs servidos de
   /sites/default/files/concursos/*.pdf, nomes descritivos. Quando a
   prova é aplicada surge a seção "Provas e Gabaritos" na própria página.
   - [X] Copiar spiders/exemplo_banca.py → spiders/fgv.py
   - [X] Listagem /concursos extrai os slugs disponíveis
         FEITO 18/08: modo descoberta (-a descoberta=1) percorre a listagem
         com paginação (a[rel=next], ?page=N), extrai slug+rótulo dos cards
         (a[href^="/concursos/"][hreflang]) e grava JSONL em
         eventos_debug/descoberta/fgv.jsonl. Validado no site real:
         339 slugs em 13 páginas.
   - [X] Filtrar PDFs pelo path /concursos/ (rodapé tem termos de uso e
         aviso de cookies que NÃO são do concurso)
   - [X] Ignorar "Vista de Prova" (espelho do candidato, exige CPF)
   - [X] Classificação de papel: hoje (08/08) o fgv.py usa cópias LOCAIS
         de regex (_RE_GAB_*, eh_relevante) — mais estritas e CORRETAS
         para o padrão FGV ("Gabarito Oficial Preliminar"). Migrar para
         heuristics.py (ADR-0004) DEPOIS de corrigir o bug do "oficial"
         no módulo (ver item em Pipeline/correções), promovendo as
         regex do FGV a parâmetros nomeados
         FEITO 13/08: fgv.py herda LexCorpusSpider e repassa regex FGV
         pelos hooks RE_*; relevância preserva gabaritos retificados.
   - [X] Montar o item via make_item da base fina (hoje o fgv.py monta
         o ArquivoItem na mão)
         FEITO 13/08: make_item ganhou override opcional de slug canônico
         para preservar -a concurso= (ex.: cnu_2025).
   - [X] Teste de parse com fixture (modelo: tests/test_pci_parse.py)
         FEITO 13/08: tests/test_fgv_parse.py cobre bloco, tipos,
         gabarito oficial preliminar, definitivo retificado, descarte e dedup.
   Notas: aceita -a url= para páginas fora de /concursos/ (o cpnu2 vive
   na raiz do site); CNU 2025 ensaiado (eventos_debug/cnu_2025.json);
   nomes opacos (b1101.pdf) viram nomes semânticos {concurso}_{bloco}_
   tipo_{n}.pdf. Quick win: HTTP puro, sem browser.
** FEITO base.py alinhada ao ADR-0004 (delegação, sem duplicação)  :spiders:
   CLOSED: 2026-08-09
   Revisão do commit 5348271 constatou que base.py duplicava regex e
   lógica de classificação (cópia do regime FRACO) em vez de delegar —
   duas fontes da verdade e override de classe com semântica divergente
   do módulo. Refactor: base.py virou base fina de fato — make_item +
   classificar_papel/eh_relevante que DELEGAM a heuristics.py,
   repassando os hooks RE_* de classe como parâmetros nomeados. Hook
   sobrescrito = regime FORTE (igual ao parâmetro do módulo; caso FCC).
   Validado: paridade base↔módulo em 7 casos, override de classe forte
   (resultado_preliminar_prova_objetiva → None), estrategia e
   agregador_generico funcionando via herança, suíte pytest 5/5.
   Pendente identificado na mesma revisão: bug do "oficial" (item
   próprio, [#A]) — agora existe em UMA cópia só (o módulo).
** FEITO Heurísticas em módulo + base fina desacoplada (ADR-0004)    :spiders:
   CLOSED: 2026-08-07
   ERRATA (09/08): parcialmente incorreto na data do fechamento — o
   módulo heuristics.py existia como descrito, mas base.py NÃO era base
   fina com delegação (tinha regex e lógica próprias). Alinhamento feito
   em 09/08 — ver "base.py alinhada ao ADR-0004" abaixo. Registro
   original mantido.
   lexcorpus/heuristics.py: regex de papel (RE_GAB_DEF, RE_GAB_PRE,
   RE_GAB, RE_PROVA, RE_DESCARTAR), classificar_papel() e eh_relevante()
   como funções puras, override por parâmetro nomeado. spiders/base.py
   virou base fina opcional: make_item (ArquivoItem, contrato v2.0) +
   delegação ao módulo com hooks RE_* de classe. Primeiro desenho
   (heurísticas na própria base) foi descartado por acoplamento —
   decisão e alternativas no ADR-0004.
** FEITO cebraspe.py refatorado (base fina + heuristics.py)          :spiders:
   CLOSED: 2026-08-07
   ERRATA (09/08): esta refatoração NÃO chegou ao código — cebraspe.py
   segue herdando scrapy.Spider, com classificar_papel local e item
   montado manualmente. Trabalho real fica no item "Alinhar base.py com
   o ADR-0004". Registro original (não realizado) abaixo.
   Removidas as cópias locais de regex e a montagem manual do item
   (~40 linhas): classificação via heuristics.py (delegação da base),
   item via make_item. Comportamento preservado: mesmo endpoint da API,
   mesmo CDN, slug canônico via eventoURL (item["concurso"] sobrescrito
   após make_item).
** FEITO Spider agregador_generico (spiders/)                        :spiders:
   CLOSED: 2026-08-07
   Varre sites agregadores SEM código novo: start_url, post_selector,
   pdf_selector, banca e concurso vêm de -a. Herda da base. Dedup de
   PDFs no nível do spider; nome do arquivo prefere o da URL (texto do
   link truncado só como fallback). Pendente: validação em crawl real
   (ver item próprio em "Em andamento / próximos").
** FEITO Spider estrategia — código (spiders/estrategia.py)          :spiders:
   CLOSED: 2026-08-07
   Blog do Estratégia Concursos: descobre posts na listagem/busca,
   pagina (rel=next), extrai PDFs do post, infere banca do título
   (_inferir_banca com lista de bancas conhecidas). Overrides via
   -a banca/-a concurso. Pendente: validar seletores contra o HTML real
   (ver item próprio em "Em andamento / próximos").
** FEITO Contrato v2.0 + schemas (evento/sidecar)                  :contrato:
   Modelo plano + metadado rico (cargos[], tipo_prova, multi_cargo,
   vigente, substituido_por). Fonte da verdade: docs/CONTRATO.md.
** FEITO Spider CEBRASPE via API oficial                            :spiders:
   Sem browser: apis.cebraspe.org.br/cebraspe/eventos/{slug} +
   cdn.cebraspe.org.br. Classificação por descricaoArquivo (não
   heurística). Concurso PRF 2021: 20 arquivos (11 provas, 6 gabaritos
   definitivos, 3 preliminares). A "refatoração para a base em 07/08"
   antes registrada aqui não se concretizou (errata 09/08 — ver o item
   "cebraspe.py refatorado" acima).
** FEITO Spider FCC (concursosfcc.com.br)                           :spiders:
   CLOSED: 2026-08-05
   spiders/fcc.py testado de ponta a ponta (dpeba125, pbaru125):
   desempacota o wrapper rybena (?file=), extrai rótulo do <title>,
   slug/ano do path (dpeba125 → 2025). Coleta gabaritos oficiais
   públicos (editais de divulgação/alteração de gabarito). Cadernos de
   prova NÃO são públicos (portal do candidato, hash individual) —
   provas FCC seguem dependendo do PCI (ver item BLOQUEADO + ADR-0001).
   Evento v2.0 validado: papel=gabarito_definitivo, cargos ["*"],
   multi_cargo=true. robots.txt da FCC bloqueia tudo → crawl com
   -s ROBOTSTXT_OBEY=False (decisão documentada no docstring).
** FEITO Pipelines: download → sidecar → evento RabbitMQ           :pipeline:
   FilesPipeline (pasta plana {banca}/{concurso}/) → SHA-256 + .meta.json
   atômico → agregação por concurso + publicação (delivery_mode=2).
** FEITO Teste e2e LexCorpus → LexLearn APROVADO (04/08)        :integracao:
   Fluxo completo com dados reais validado pelo consumidor de teste.
   Registro: docs/COMUNICADO_INTEGRACAO_OK.md.
** FEITO Settings por ambiente (RABBIT_ENABLED/RABBIT_URL via env)   :pipeline:
   CLOSED: 2026-08-05
   Patch de 2 linhas: os.environ.get com defaults inalterados
   (False / localhost). Habilita o container Docker a ligar publicação
   via compose sem tocar código. Testado: dev local, container e typo
   na variável (cai seguro para False).
** FEITO Dockerfile + .dockerignore na raiz do LexCorpus              :docker:
   CLOSED: 2026-08-05
   python:3.11-slim, ENTRYPOINT scrapy (CMD default: list),
   LEXCORPUS_FILES_STORE=/data/raw/exams. Container efêmero por crawl
   (run --rm), não é serviço permanente. Stage playwright fica para o
   item multi-stage.
** FEITO docker-compose.crawler.yml (integração LexLearn-v3)          :docker:
   CLOSED: 2026-08-05
   ERRATA (01/09): o arquivo mudou de casa — desde 01/09 vive na raiz
   DESTE repo, com o docker-compose.lexlearn.yml de ponte ao lado (e o
   LEXCORPUS_DIR morreu). Registro original mantido abaixo; o estado
   vigente está no checkbox "COMPOSE MUDOU DE CASA" do item
   "docker-compose de dev/prod".
   Override aditivo na raiz do LexLearn-v3 — docker-compose.base.yml
   intocado. Profile "crawler" (não sobe com o up normal), mesma rede
   do projeto lexlearn-v3 (resolve rabbitmq pelo nome), mesmo bind
   ./data:/data do worker (pasta_uri do contrato §7 resolve igual nos
   dois lados). Pendente: LEXCORPUS_DIR no .env e teste e2e via compose
   (acompanhar no item "docker-compose de dev/prod").
** FEITO Diagnóstico das 4 fontes (05/08)                        :pesquisa:
   FGV e FCC: estáticas, sem captcha (quick wins). Cesgranrio: 403 do
   Azure Front Door. PCI: Turnstile + robots.txt Disallow /*.pdf →
   reclassificado como fonte de descoberta apenas (ADR-0001).
** CANCELADO Heurísticas como módulo solto (lexcorpus/heuristics.py) :spiders:
   CLOSED: 2026-08-07
   ERRATA (09/08): este cancelamento foi REVERTIDO pelo ADR-0004 no mesmo
   dia — o módulo heuristics.py EXISTE, é a decisão vigente e a fonte da
   verdade das heurísticas. Entrada mantida apenas como histórico da
   oscilação de desenho (módulo → classe base → módulo).
   Substituído pela classe base LexCorpusSpider (spiders/base.py), que
   centraliza regex + classificar_papel + make_item e dá override por
   herança. O que restava do escopo (migrar pci.py/fcc.py, testes) virou
   o item "Migrar pci.py e fcc.py para LexCorpusSpider".
